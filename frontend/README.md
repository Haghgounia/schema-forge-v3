# SchemaForge v3 UI

رابط کاربری مستقل React + TypeScript + Vite است و از طریق `/api` به Backend متصل می‌شود.

```bash
npm install
npm run dev
```

در محیط توسعه، درخواست‌های `/api` به `http://localhost:9090` ارسال می‌شوند.
